// Global head-only styles.
//
// Purpose:
// - This file is ALWAYS included into <head>.
// - It is NOT filtered, so you can put here non-@media rules (e.g. Outlook fixes).
// - It does NOT participate in CSS inlining.
//
// Keep it minimal.

// Outlook/base resets (head-only)
@import 'helpers/outlook-reset'
