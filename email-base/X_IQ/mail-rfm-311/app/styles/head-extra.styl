// Per-mail head-extra styles.
// This file is included in <head> AND participates in inlining.
// Use for "must-have" rules that should exist in both places.
// Examples:
// - Base typography defaults for clients that ignore inline
// - Utility classes referenced by media queries
@import 'blocks/head-extra'
