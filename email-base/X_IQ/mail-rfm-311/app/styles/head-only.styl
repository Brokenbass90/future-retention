// Per-mail head-only styles.
// Put here ONLY styles that must stay in <head> (not inlined).
// Examples:
// - @font-face
// - Outlook-specific fixes (@supports / mso selectors)
// - Critical media queries that must remain in head
